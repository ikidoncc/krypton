{
  "name": "krypton",
  "private": true,
  "version": "0.0.1",
  "description": "Multiplayer word deduction game — P2P via WebRTC",
  "scripts": {
    "dev": "pnpm --filter @krypton/web dev",
    "build": "pnpm --filter @krypton/web build",
    "test": "pnpm --filter @krypton/engine test",
    "typecheck": "pnpm --filter @krypton/shared exec tsc --build && pnpm --filter @krypton/engine exec tsc --noEmit && pnpm --filter @krypton/network exec tsc --noEmit && pnpm --filter @krypton/web exec tsc --noEmit",
    "clean": "pnpm -r exec rm -rf dist node_modules"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
